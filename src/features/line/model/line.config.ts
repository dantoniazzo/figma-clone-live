import { type LineCap } from "konva/lib/Shape";

export const LineConfig = {
  tension: 0.5,
  bezier: true,
  lineCap: "round" as LineCap,
  stroke: "white",
  strokeWidth: 2,
  hitStrokeWidth: 20,
};
